import $ from 'jquery'

import * as Rx from 'rxjs';

export class Home {

    attached() {
        // $('#resizeable').resizable({
        //     handles: "n, e, s, w"
        // })
        // let o = Rx.Observable.of(true);
        // console.log(o);
    }
    
}